spring:
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      lettuce:
        cluster:
          refresh:
            adaptive: true
            period: 30s

logging:
  level:
    root: info
